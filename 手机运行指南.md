# 手机运行指南（最简单，无需 APK）

## 步骤

### 1. 安装 Pydroid 3
在安卓手机上打开应用商店，搜索并安装 **Pydroid 3**（免费，约 30MB）。

### 2. 复制代码文件到手机
把这两个文件传到手机（微信文件传输、QQ、数据线都可以）：
- `main.py`
- `db.py`

在 Pydroid 3 中打开 `main.py`。

### 3. 安装依赖
点击 Pydroid 3 右上角的 **菜单 -> 终端**，在终端中输入：

```bash
pip install flet plotly pandas
```

等待安装完成（约 1-2 分钟，需要网络）。

### 4. 运行
点击 Pydroid 3 的 **黄色播放按钮**（▶️）运行。

程序启动后，在终端日志中会显示一个本地地址，例如 `http://localhost:53892`。点击它，或者在手机浏览器中打开该地址，即可使用。

### 5. 日常使用
- 头晕时打开 Pydroid 3，点击播放按钮运行
- 应用界面会在浏览器中打开
- 数据自动保存在手机本地

---

## 如果坚持要 APK

由于当前环境网络限制无法完成本地打包，推荐使用 **GitHub Actions 在线打包**：

1. 把本项目的所有文件（main.py, db.py, requirements.txt）上传到一个 GitHub 仓库
2. 在仓库中创建 `.github/workflows/build.yml`（我已写好模板在 README_APK.md 中）
3. 在 GitHub 网页上点击 Actions -> 运行工作流
4. 等待约 10-15 分钟后，直接下载生成的 APK 文件

这种方式不需要在你的电脑上安装任何东西。
